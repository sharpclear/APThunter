# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
.venv
*.egg-info
dist/
build/
.pytest_cache/
.coverage
htmlcov/
*.log
.DS_Store

# IDE
.vscode/
.idea/
*.swp
*.swo

# Git
.git
.gitignore

# Documentation
README.md
*.md
!报告模板/
!报告模板/*.md

# Environment
.env
.env.local
.env.*.local

# Data and models (if large, consider mounting as volumes)
# fastText 预训练向量约 9GB，当前线上检测/订阅链路不依赖，避免进入构建上下文。
app/models/fasttext/

# Compose 会把宿主机 daily_data 挂载到 /app/app/daily_data，历史日数据不需要打进镜像。
app/daily_data/

# CANINE 深度模型不是默认业务链路依赖，避免 1GB 级模型文件进入镜像上下文。
app/models/pretrained_model/canine-s/

# 训练产生的动态模型通过 Compose 挂载 saved_model 目录持久化，镜像只保留官方基线模型文件。
app/models/saved_model/model_TRAIN_*.pkl
app/models/saved_model/scaler_TRAIN_*.pkl
app/models/saved_model_ML/

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db
