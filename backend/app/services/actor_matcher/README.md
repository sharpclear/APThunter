# 旧组织归因兼容模块

## 状态

本目录对应的组织归因、组织关联和组织画像方法已在产品侧标记为**未在使用、待整体替换**。

部分历史检测任务、预警结果和接口仍可能调用这里的代码，因此当前保留其兼容行为。保留不代表它是新方案的设计依据。

## 开发约束

- 不要在新功能中依赖这里的匹配分数、画像缓存、证据格式或组织结论。
- 不要继续向这里增加归因规则、画像字段或新的检测模块接入。
- 域名追踪模块采集的 DNS、WHOIS、证书、TLS 和网页应用指纹应保持独立，只保存可复用事实，不调用本目录生成组织结论。
- 待新的归因方案确定后，再以独立迁移方案替换历史调用和数据结构。
