# APTHunter 恶意域名检测报告

## 一、任务基本信息

| 项目 | 内容 |
| --- | --- |
| 报告名称 | APTHunter 恶意域名检测报告 |
| 任务编号 | {{ task_id | mdcell }} |
| 数据来源 | {{ data_source_label | mdcell }} |
| 日期范围 | {% if date_range %}{{ date_range[0] | mdcell }} 至 {{ date_range[1] | mdcell }}{% else %}-{% endif %} |
| 报告生成时间 | {{ generated_at | mdcell }} |

## 二、执行摘要

本次任务共检测 **{{ total_count }}** 个域名，四个检测模块包括仿冒域名检测、DGA域名检测、历史APT域名相似性检测和模板化APT域名检测。

统一判定规则为：只要任一模块判定域名命中风险，该域名即归入恶意域名；仅当四个模块均未判定为恶意时，才归入正常域名。本次共识别恶意域名 **{{ malicious_count }}** 个，正常域名 **{{ normal_count }}** 个，恶意域名占比 **{{ malicious_rate }}**。

## 三、总体统计

| 指标 | 数值 |
| --- | --- |
| 总域名数 | {{ statistics["总域名数"] }} |
| 恶意域名数 | {{ statistics["恶意域名数"] }} |
| 正常域名数 | {{ statistics["正常域名数"] }} |
| 恶意域名占比 | {{ statistics["恶意域名占比"] }} |

## 四、模块命中分布

| 检测模块 | 调用模型 | 命中域名数 | 占总域名比例 |
| --- | --- | ---: | ---: |
{% for item in module_summaries -%}
| {{ item.module | mdcell }} | {{ item.model_name | mdcell }} | {{ item.count }} | {{ item.percent }} |
{% endfor %}

## 五、多标签交叉命中

| 恶意类别组合 | 域名数 | 占恶意域名比例 |
| --- | ---: | ---: |
{% for item in overlap_summaries -%}
| {{ item.labels | mdcell }} | {{ item.count }} | {{ item.percent }} |
{% endfor %}

## 六、恶意域名明细

| 域名 | 恶意类别标签 | 命中模块数 | 关键命中详情 |
| --- | --- | ---: | --- |
{% for item in top_domains -%}
| {{ item["域名"] | mdcell }} | {{ item["恶意类别"] | mdcell }} | {{ item["命中模块数"] }} | {{ item["命中详情"] | mdcell }} |
{% endfor %}

## 七、DGA域名检测结果

本次共识别 DGA 域名 **{{ dga_result_count }}** 个，其中 **{{ dga_actor_attribution_count }}** 个域名可根据已识别的 DGA 家族关联到公开组织线索。下表依次展示“已识别家族且存在组织线索”“已识别家族但暂无组织线索”和“未知家族”三类结果。

{% if dga_result_rows %}
| DGA域名 | DGA分数 | DGA家族 | APT组织名 | 关联类型 |
| --- | ---: | --- | --- | --- |
{% for item in dga_result_rows -%}
| {{ item.domain | mdcell }} | {{ item.score }} | {{ item.family | mdcell }} | {{ item.apt_organization_names | mdcell }} | {{ item.apt_relationship_types_cn | mdcell }} |
{% endfor %}
{% else %}
本次任务未识别出高置信 DGA 域名。
{% endif %}

## 八、DGA家族与APT组织关系说明

{% if actor_relationship_overview %}
| DGA家族 | APT组织 | 关系说明 |
| --- | --- | --- |
{% for item in actor_relationship_overview -%}
| {{ item.family | mdcell }} | {{ item.apt_organization_name | mdcell }} | {{ item.relationship_explanation_cn | mdcell }} |
{% endfor %}
{% else %}
本次检测结果中暂无可展示的DGA家族与APT组织关系。
{% endif %}

## 九、模块阈值与策略

| 模块 | 策略 |
| --- | --- |
| 仿冒域名检测 | 使用系统全量官方白名单，采用当前仿冒检测模型默认自适应阈值。 |
| DGA域名检测 | DGA候选阈值：{{ thresholds.get("dga_candidate_threshold", "-") }}。 |
| 历史APT域名相似性检测 | 最低相似度阈值：{{ thresholds.get("history_min_score", "-") }}，每个域名保留 Top {{ thresholds.get("history_top_k", "-") }} 匹配。 |
| 模板化APT域名检测 | 模板命中阈值：{{ thresholds.get("apt_template_score_threshold", "-") }}。 |

## 十、边界说明

1. 本报告仅汇总本次四个域名检测模块的模型输出，不替代人工复核和处置确认。
2. 正常域名仅表示本次未被四个模块命中，不代表永久安全。
3. 同一域名可能同时命中多个恶意类别，报告按多标签方式保留所有命中类别。
4. 若任一必选检测模块执行失败，本次任务应整体失败，不输出部分模块结果作为最终结论。
